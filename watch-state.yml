version: "1.0"
created: 2025-10-15T16:58:27.157109897-04:00
updated: 2025-10-15T21:17:24.121958428-04:00
monitors:
    https://httpbin.org/delay/1:
        name: Slow Service
        url: https://httpbin.org/delay/1
        method: GET
        threshold: 45
        status_codes:
            - '*'
        size_alerts:
            enabled: true
            history_size: 100
            threshold: 0.5
        check_strategy: http
        alert_strategy: console
    https://httpbin.org/status/200:
        name: Monitor-https://httpbin.org/status/200
        url: https://httpbin.org/status/200
        method: GET
        threshold: 30
        status_codes:
            - '*'
        size_alerts:
            enabled: true
            history_size: 100
            threshold: 0.5
        check_strategy: http
        alert_strategy: console
    https://httpbin.org/status/201:
        name: Monitor-https://httpbin.org/status/201
        url: https://httpbin.org/status/201
        method: GET
        threshold: 25
        status_codes:
            - '*'
        size_alerts:
            enabled: true
            history_size: 100
            threshold: 0.5
        check_strategy: http
        alert_strategy: console
    https://httpbin.org/status/404:
        name: Monitor-https://httpbin.org/status/404
        url: https://httpbin.org/status/404
        method: POST
        threshold: 60
        status_codes:
            - '*'
        size_alerts:
            enabled: true
            history_size: 100
            threshold: 0.5
        check_strategy: http
        alert_strategy: console
settings:
    webhook_port: 8080
    webhook_path: /webhook
    check_interval: 5
    default_threshold: 30
